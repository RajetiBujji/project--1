#include <stdio.h>

int main() {
    int num = 3278;
    int rem, sum = 0;
    
    while (num != 0) {
        rem = num % 10;
        sum += rem;
        num /= 10;
    }
    
    printf("Sum of the digits: %d\n", sum);
    
    return 0;
}